# ---- Node / JS ----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo

# Build outputs
backend/dist/
frontend/dist/

# Vite/ tooling caches
.vite/
frontend/.vite/
backend/.vite/

# ---- Env files (never commit secrets) ----
.env
.env.local
.env.*.local
backend/.env
frontend/.env

# ---- Logs ----
logs/
*.log

# ---- OS / Editor junk ----
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
.history/

# ---- Docker ----
# keep Dockerfile and docker-compose.yml tracked
docker-compose.override.yml
docker-compose.*.yml
# if someone bind-mounts a pgdata folder locally (we use a named volume by default)
pgdata/

# ---- Terraform (ignore anywhere in repo) ----
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
*.auto.tfvars
*.auto.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
terraform.tfplan
*.tfplan
.terraformrc
terraform.rc
!.terraform.lock.hcl
# If you keep example vars in Git, un-comment the next line:
# !*.tfvars.example

*.zip